import { LinesVisualizerDirective } from './lines-visualizer.directive';

describe('LinesVisualizerDirective', () => {
  it('should create an instance', () => {
    // const directive = new LinesVisualizerDirective();
    // expect(directive).toBeTruthy();
  });
});
