import { CircleVisualizerDirective } from './circle-visualizer.directive';

describe('CircleVisualizerDirective', () => {
  it('should create an instance', () => {
    const directive = new CircleVisualizerDirective();
    expect(directive).toBeTruthy();
  });
});
