import { VisualizerDirective } from './visualizer.directive';

describe('VisualizerDirective', () => {
  it('should create an instance', () => {
    const directive = new VisualizerDirective();
    expect(directive).toBeTruthy();
  });
});
