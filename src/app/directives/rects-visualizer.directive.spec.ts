import { RectsVisualizerDirective } from './rects-visualizer.directive';

describe('RectsVisualizerDirective', () => {
  it('should create an instance', () => {
    const directive = new RectsVisualizerDirective();
    expect(directive).toBeTruthy();
  });
});
